import 'package:flutter/material.dart';

/// Thème « Abidjan Trajet » — mobilité urbaine et suggestions d’itinéraires (IA).
class Constants {
  static final Color primaryColor = Color.fromRGBO(15, 42, 52, 1);
  static final Color greyColor = Color.fromRGBO(245, 247, 250, 1);
  static final Color accentOrange = Color(0xFFFE7A3B);
  static final Color accentGreen = Color(0xFF2D9D78);
  /// Accent « lagon / IA » (connexions, itinéraires intelligents).
  static final Color accentLagoon = Color(0xFF0D9488);
  static final Color backgroundDark = Color(0xFF0F2A34);

  /// Centre carte par défaut (Abidjan).
  static const double abidjanLat = 5.3599;
  static const double abidjanLng = -4.0083;

  /// Teal type tableau de bord (écrans options / accueil).
  static const Color dashboardTeal = Color(0xFF48C9B0);
}
